-- This file is no longer needed as the dedicated PostgreSQL service
-- automatically creates the database and user.

-- If you need to manually initialize the database, run:
-- docker-compose exec mastodon-db psql -U mastodon -d mastodon
